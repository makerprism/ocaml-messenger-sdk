# messenger-lwt

Lwt-focused adapter scaffold for `ocaml-messenger-sdk`.

This package currently provides lightweight placeholder types and helper functions intended to back future `messenger-core` HTTP operations.
