# messenger-lwt

Lwt-focused adapter scaffold for `ocaml-messenger-sdk`.

This package currently provides Lwt adapters for CPS-style core interfaces plus a stub HTTP backend (`Http_client_stub`).

It does not yet ship a production-ready Cohttp/Eio HTTP implementation.
